# Web Application

This folder contains the Angular 20 frontend application for the AI Interview Coach platform.
It hosts the presentation layer, feature modules, shared UI primitives, and application state.
