# Shared Packages

This folder contains reusable shared libraries for types, UI components, linting rules, and TypeScript configuration.
The packages are intended to be consumed by the web and API applications.
